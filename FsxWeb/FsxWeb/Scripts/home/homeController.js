(function (angular) {
    'use strict';

    var app = angular
        .module('fsxWebApp')
        .controller('HomeController', HomeController);

    HomeController.$inject = ['$scope'];

    function HomeController($scope) {
        
    }

})(window.angular)